# Extra dependencies for backend development and testing only.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
# httpx is required by fastapi.testclient.TestClient
httpx==0.28.1